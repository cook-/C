/* sort the elements of an array from largest to smallest */
#include <stdio.h>
main()
{
	printf("Input several numbers you want to sort(enter to the end): ");
	double dval;
	while (scanf("%lf", &dval))
}
